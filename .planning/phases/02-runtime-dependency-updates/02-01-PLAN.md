---
phase: 02-runtime-dependency-updates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/kotti/tests/test_sanitizers.py
  - src/kotti/__init__.py
  - src/kotti/migrate.py
  - docs/conf.py
autonomous: true
requirements:
  - DEP-02
  - DEP-03
  - DEP-04
  - DEP-05
  - DEP-06
  - DEP-07
  - DEP-08

must_haves:
  truths:
    - "Characterization tests capture current bleach sanitizer behavior as baseline for migration"
    - "pkg_resources is not imported anywhere in src/kotti/ or docs/"
    - "get_version() returns the correct package version without pkg_resources"
    - "KOTTI_SCRIPT_DIR resolves to the kotti/alembic directory via importlib.resources"
    - "No 'from mock import' or 'import mock' (the PyPI package) exists in any source file"
    - "pyramid>=1.9,<2 and sqlalchemy>=1.4,<2 pins are present in pyproject.toml"
  artifacts:
    - path: "src/kotti/tests/test_sanitizers.py"
      provides: "Characterization tests capturing bleach behavior with DOM-equivalence approach"
    - path: "src/kotti/__init__.py"
      provides: "get_version() using __version__ (importlib.metadata), no pkg_resources import"
    - path: "src/kotti/migrate.py"
      provides: "KOTTI_SCRIPT_DIR using importlib.resources.files(), no pkg_resources import"
    - path: "docs/conf.py"
      provides: "Version lookup using importlib.metadata, no pkg_resources import"
  key_links:
    - from: "src/kotti/migrate.py"
      to: "src/kotti/alembic/"
      via: "importlib.resources.files('kotti') / 'alembic'"
      pattern: "importlib_resources\\.files.*alembic"
    - from: "src/kotti/__init__.py"
      to: "__version__"
      via: "get_version() returns __version__"
      pattern: "def get_version.*return __version__"
---

<objective>
Establish baseline characterization tests for current bleach sanitizer behavior and migrate all pkg_resources usage to importlib equivalents.

Purpose: The characterization tests capture exact bleach behavior before the nh3 migration in Plan 02-02 — they serve as the "before" snapshot and become the permanent regression suite after rewriting for nh3. The importlib migration removes the last runtime dependency on setuptools/pkg_resources, which is deprecated and emits warnings in modern setuptools.

Output: Updated test_sanitizers.py with characterization tests, updated __init__.py/migrate.py/docs/conf.py with importlib replacements, confirmed mock/pin verification.
</objective>

<execution_context>
@/Users/disko/.claude/get-shit-done/workflows/execute-plan.md
@/Users/disko/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-runtime-dependency-updates/02-RESEARCH.md

@src/kotti/sanitizers.py
@src/kotti/tests/test_sanitizers.py
@src/kotti/__init__.py
@src/kotti/migrate.py
@docs/conf.py

<interfaces>
<!-- Key interfaces the executor needs -->

From src/kotti/sanitizers.py:
- xss_protection(html: str) -> str — uses bleach.clean with generally_xss_safe tags, lambda attributes, all_styles
- minimal_html(html: str) -> str — uses bleach.clean with markdown_tags + print_tags, merged attrs dict, styles=[]
- no_html(html: str) -> str — uses bleach.clean with tags=[], attributes={}, styles=[]
- sanitize(html: str, sanitizer: str) -> str — dispatches to named sanitizer from settings
- _setup_sanitizers(settings) — resolves dotted name sanitizer strings to callable functions
- _setup_listeners(settings) — wires ObjectInsert/ObjectUpdate event listeners for sanitize-on-write

From src/kotti/__init__.py (lines 1-10, 170-171):
- __version__ = _version("Kotti") via importlib.metadata (already set at module top)
- import pkg_resources (line 10) — STILL PRESENT, to be removed
- get_version() (line 170) — returns pkg_resources.require("Kotti")[0].version — TO BE REPLACED

From src/kotti/migrate.py (lines 45, 57):
- import pkg_resources (line 45) — TO BE REPLACED
- KOTTI_SCRIPT_DIR = pkg_resources.resource_filename("kotti", "alembic") (line 57) — TO BE REPLACED

From docs/conf.py (lines 15, 39):
- import pkg_resources (line 15) — TO BE REPLACED
- version = pkg_resources.get_distribution("Kotti").version (line 39) — TO BE REPLACED
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write bleach characterization tests and verify mock/pin status</name>
  <files>src/kotti/tests/test_sanitizers.py</files>
  <action>
  Rewrite the verification helper functions in test_sanitizers.py to serve as characterization tests that capture the CURRENT bleach behavior. These tests will be the "before" baseline — Plan 02-02 will update them for nh3 behavior.

  **Characterization test approach (from CONTEXT.md):** Tests compare parsed DOM behavior, not raw string output. Minor whitespace/quoting differences are acceptable — what matters is that HTML renders the same in a browser. However, for this task (baseline capture), record the EXACT bleach output since we ARE testing bleach right now. The DOM-equivalence approach becomes relevant when rewriting for nh3 in Plan 02-02.

  **Changes to test_sanitizers.py:**

  1. Add a new test function `test_bleach_characterization_xss_protection()` that captures exact bleach behavior for the `unsanitized` test fixture:
     - Script tags: bleach strips `<script>` tags but PRESERVES content — assert `"alert('XSS!')" in sanitized`
     - Attribute handling: bleach with `lambda self, key, value: True` preserves ALL attributes — assert `'size="17"' in sanitized`
     - Style preservation: bleach with `all_styles` preserves style values — assert `'style="color: red"' in sanitized`
     - Link handling: bleach does NOT add rel attributes — assert `'target="_blank"' in sanitized` and `'rel=' not in sanitized`
     - Tag handling: `<marquee>` is NOT in generally_xss_safe, so stripped — assert `'<marquee>' not in sanitized`

  2. Add `test_bleach_characterization_minimal_html()`:
     - Style handling: bleach with `styles=[]` strips CSS but leaves empty attribute — assert `'style=""' in sanitized`
     - Attribute stripping: size attribute on `<b>` is NOT in markdown_attrs or print_attrs — assert `'size' not in sanitized.lower()`

  3. Add `test_bleach_characterization_no_html()`:
     - All tags removed, content preserved — assert `'<' not in sanitized` and `'external links' in sanitized`

  4. Keep ALL existing tests unchanged (test_no_html, test_minmal_html, test_xss_protection, test_default_config, test_setup_sanitizers, test_listeners, test_sanitize). The characterization tests are ADDITIONS, not replacements.

  **Grep sweep for mock (DEP-03 verification):**
  Run: `grep -rn "^from mock import\|^import mock$" src/kotti/`
  Expected result: zero matches. The one legitimate occurrence is `import unittest.mock as mock` in test_register.py — that is NOT the PyPI mock package.

  **Pin verification (DEP-07, DEP-08):**
  Run: `grep "pyramid>=1.9,<2" pyproject.toml` — must match
  Run: `grep "sqlalchemy>=1.4" pyproject.toml` — must match
  These are verification-only, no changes needed.
  </action>
  <verify>
    <automated>cd /Users/disko/Projects/ipn/Kotti && uv run pytest src/kotti/tests/test_sanitizers.py -x -q</automated>
  </verify>
  <done>
  - Characterization tests added that capture exact bleach behavior for all 3 sanitizers
  - All existing sanitizer tests still pass
  - Grep confirms zero `from mock import` / `import mock` (PyPI package) in source
  - Grep confirms pyramid and sqlalchemy pins present in pyproject.toml
  </done>
</task>

<task type="auto">
  <name>Task 2: Replace pkg_resources with importlib in __init__.py, migrate.py, and docs/conf.py</name>
  <files>src/kotti/__init__.py, src/kotti/migrate.py, docs/conf.py</files>
  <action>
  Replace all remaining `pkg_resources` usage with importlib equivalents. Three files need changes:

  **src/kotti/__init__.py:**
  1. Remove `import pkg_resources` (line 10)
  2. Replace `get_version()` function body (around line 170-171):
     ```python
     def get_version():
         return __version__
     ```
     The `__version__` is already set at the top of the file via `importlib.metadata.version("Kotti")`. The `get_version()` function now simply returns it. No new import needed.

  **src/kotti/migrate.py:**
  1. Replace `import pkg_resources` (line 45) with:
     ```python
     import importlib.resources as importlib_resources
     ```
  2. Replace `KOTTI_SCRIPT_DIR = pkg_resources.resource_filename("kotti", "alembic")` (line 57) with:
     ```python
     KOTTI_SCRIPT_DIR = str(importlib_resources.files("kotti") / "alembic")
     ```
     Per research: this returns a real filesystem path because hatchling builds do NOT zip packages. The existing test assertion `.endswith("kotti/alembic")` remains valid. No `as_file()` context manager needed.

  **docs/conf.py:**
  1. Replace `import pkg_resources` (line 15) with:
     ```python
     from importlib.metadata import version as _pkg_version
     ```
  2. Replace `version = pkg_resources.get_distribution("Kotti").version` (line 39) with:
     ```python
     version = _pkg_version("Kotti")
     ```

  **Post-change verification:**
  Run `grep -rn "import pkg_resources" src/kotti/ docs/` — must return zero results.

  **DO NOT touch pyproject.toml in this task.** The setuptools pin removal and bleach->nh3 swap happen together in Plan 02-02 to keep the lockfile update atomic.
  </action>
  <verify>
    <automated>cd /Users/disko/Projects/ipn/Kotti && uv run pytest src/kotti/tests/test_migrate.py src/kotti/tests/test_sanitizers.py -x -q && grep -rn "import pkg_resources" src/kotti/ docs/ && echo "FAIL: pkg_resources still found" || echo "PASS: no pkg_resources found"</automated>
  </verify>
  <done>
  - src/kotti/__init__.py: no pkg_resources import, get_version() returns __version__
  - src/kotti/migrate.py: uses importlib.resources.files() for KOTTI_SCRIPT_DIR
  - docs/conf.py: uses importlib.metadata.version() for version lookup
  - test_migrate.py passes (KOTTI_SCRIPT_DIR resolves correctly)
  - Zero grep hits for "import pkg_resources" in src/kotti/ and docs/
  </done>
</task>

</tasks>

<verification>
Phase-level checks after this plan completes:

1. `uv run pytest src/kotti/tests/test_sanitizers.py -x -q` — all tests pass (existing + characterization)
2. `uv run pytest src/kotti/tests/test_migrate.py -x -q` — migration tests pass with importlib.resources
3. `grep -rn "import pkg_resources" src/kotti/ docs/` — zero results
4. `grep -rn "^from mock import\|^import mock$" src/kotti/` — zero results
5. `grep "pyramid>=1.9,<2" pyproject.toml` — pin present
6. `grep "sqlalchemy>=1.4" pyproject.toml` — pin present
</verification>

<success_criteria>
- Characterization tests capture bleach baseline behavior for all 3 sanitizers (xss_protection, minimal_html, no_html)
- All existing sanitizer tests still pass unchanged
- pkg_resources fully eliminated from src/kotti/ and docs/
- importlib.resources correctly resolves Alembic script directory
- get_version() works without pkg_resources
- Mock and pin verification confirmed
</success_criteria>

<output>
After completion, create `.planning/phases/02-runtime-dependency-updates/02-01-SUMMARY.md`
</output>
