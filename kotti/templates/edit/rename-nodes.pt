<!DOCTYPE html>
<html xmlns="http://www.w3.org/1999/xhtml"
      xmlns:i18n="http://xml.zope.org/namespaces/i18n"
      i18n:domain="Kotti"
      metal:use-macro="api.macro('kotti:templates/edit/master.pt')">

  <div metal:fill-slot="content">
    <h1 i18n:translate="">Rename</h1>

    <div class="tab-pane" id="rename">
      <form action="${request.url}" method="post" id="form-move-3">
        <tal:items tal:repeat="item items">
          <input type="hidden" name="children-to-rename" value="${item.id}" />
          <div class="control-group">
            <label class="control-label desc" for="move-new-name" i18n:translate="">New name(${item.name})</label>
            <div class="controls">
              <input type="text" name="${item.id}-name" id="move-new-name"
                     value="${request.POST.get(str(item.id) + '-name') or item.name}" />
            </div>
          </div>
          <div class="control-group">         
            <label class="control-label desc" for="move-new-title" i18n:translate="">New title(${item.title})</label>
            <div class="controls">
              <input type="text" name="${item.id}-title" id="move-new-title"
                     value="${request.POST.get(str(item.id) + '-title') or item.title}" />
            </div>
          </div>
        </tal:items>
        <div class="form-actions">
          <button type="submit" name="rename" value="rename" 
                  class="btn btn-warning" i18n:translate="">Rename</button>
        </div>
      </form>    
    </div>
  </div>
</html>
